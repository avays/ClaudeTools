{
  "name": "claudetools",
  "version": "0.1.0",
  "description": "Portable Claude Code toolchain — agent pipeline, path-scoped rules, and a rule-governance system that keeps agent context from rotting.",
  "type": "module",
  "bin": { "ct": "./bin/ct" },
  "engines": { "node": ">=20" },
  "files": ["bin", "tools", "templates", "claudetools.config.schema.json", "docs", "README.md"],
  "scripts": { "check": "node tools/check.mjs --templates" },
  "license": "MIT"
}
